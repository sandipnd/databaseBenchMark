The redis test is taken from YCSB. It is modified to work for Redis cluster.
For Replica the code is same except the number of nodes are changed. Hosts on where the 
redis-server is running is hard coded.
